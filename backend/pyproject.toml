[project]
name = "agent-chat-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "deepagents",
    "aegra-api",
    "langgraph",
    "langchain-openai",
    "langchain-anthropic",
    "python-dotenv",
    "psycopg2-binary",
    "asyncpg",
]
